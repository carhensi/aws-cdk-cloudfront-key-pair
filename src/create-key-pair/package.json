{
  "name": "create-key-pair",
  "private": true,
  "version": "0.1.0",
  "description": "AWS CDK custom resource to create a key pair",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "bundle": "esbuild index.ts --outdir=dist --bundle --minify --sourcemap --platform=node --target=node24 --external:@aws-sdk/client-secrets-manager"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.147",
    "@types/jest": "^29.5.14",
    "aws-lambda": "^1.0.7",
    "esbuild": "^0.25.0",
    "jest": "^29.7.0",
    "typescript": "^5.7.3",
    "@aws-sdk/client-secrets-manager": "^3.750.0"
  }
}
